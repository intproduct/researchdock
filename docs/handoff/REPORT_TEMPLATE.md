# Txx 实现报告

> 按任务 ID 复制为 reports/Txx-implementation.md（例如 T03-implementation.md）；不要修改本模板充当交付。所有“未执行/unknown”均如实保留。这里没有占位的成功结论。

- 状态：implementing / ready_for_review / blocked_environment
- 任务卡：
- 分支：
- 实际 base SHA：
- 最后代码提交 SHA（此报告提交之前）：
- 执行平台/解释器/Node/数据库版本：
- 实际模型与服务来源（不知道写 unknown）：

## 结果

用两三句话说明用户现在能完成什么，什么仍未实现。列出主要修改文件及原因；不要粘贴整个 diff。

## 契约核对

| 任务卡验收项 | 结果（通过/失败/未执行） | 证据：测试名、日志位置或实际步骤 |
| --- | --- | --- |
| 按任务卡逐项填 | 未执行 | |

## 执行记录

| 工作目录与完整命令（去掉秘密） | 环境/实际数据库 | 退出码与测试数量 | 结果 |
| --- | --- | --- | --- |
| | | | |

UI 操作步骤及可观察结果：

迁移/恢复验证：使用的临时环境、执行顺序、数据核对结果。若没有 PostgreSQL/容器则直接写未执行。

## 设计决定与偏离

说明与任务卡不同的行为及依据。无偏离则写“无”。新增依赖逐个说明必要性，不能用“优化”代替原因。

## 阻塞和未解决事项

区分代码问题、环境缺口、任务范围外事项。写下一步需要什么具体条件。不要求用户提供密钥到聊天或报告。

## 成本记录

| 项目 | 数值 | 单位/计量来源 |
| --- | --- | --- |
| 输入总量 | unknown | tokens |
| 其中缓存命中输入 | unknown | tokens；是否包含在上行需说明 |
| 输出 | unknown | tokens |
| 推理 | unknown | tokens；与输出是否重叠需说明 |
| 实际费用或套餐用量 | unknown | 原币种/额度、计费来源、日期 |
| 修复轮数 | unknown | 轮 |
| 人类介入 | unknown | 次，简述原因 |

不要估算不存在的日志，不记录账户余额、API key 或原始对话。首次执行完再记录实际数据。

## 交接

- 尚未进行独立审计；请按 REVIEW_PROTOCOL 审计。
- 最终回复中提供包含报告的 HEAD SHA。
- 下一任务：等待审计；不要自行写 accepted。
