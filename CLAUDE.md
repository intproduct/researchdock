# Claude Code 开发入口

你在 Research Manager 独立项目中工作。先读 `AGENTS.md`、`docs/status.md`，然后读 `docs/handoff/START_HERE.zh-CN.md` 和其中指定的当前任务卡。

用户已选择：Claude Code（Kimi 后端）负责按任务实现和自测，Codex 负责契约、独立审计和验收。通过本仓库的提交与 Markdown 交接，不需要联网消息系统或额外 Agent 框架。

- 初始授权任务为 T01；T02 在 T01 审计通过并整合后开始。后续任务只有概要，未授权自动扩展实施。
- 做完一包就交付提交与报告，状态写 `ready_for_review`；实现者不得自行写 `accepted`。
- 不把计划、已有 66 项测试或自己的测试报告当作独立审计结论。
- 按具体任务卡读取相关代码，不全量重扫仓库或参考知识库。无需 clone 任何参考仓库。
- 不修改相邻 Ran-ASKS-Fang 仓库，不复制其 raw/wiki/graph 或实现代码。
- 不擅自切换模型配置、不新增外部服务、不创建或推送远端仓库。现有本地开发服务、数据库及凭据不得清空或覆盖。
- 对任务内可逆代码修改、测试和本地提交直接执行；缺少外部环境时记录阻塞及证据，完成不依赖该环境的部分。
- 交付格式用 `docs/handoff/REPORT_TEMPLATE.md`；不要将密码、Token、私有研究内容、原始会话放入报告。

上面的任务状态和顺序以用户后续明确指令及审计记录为准。
